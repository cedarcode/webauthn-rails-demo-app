<% if (message = unsupported_browser_message) %>
    <div class="mdc-list mdc-list--two-line">
      <div class="mdc-list-item">
        <span class="mdc-list-item__graphic material-icons">warning</span>
        <span class="mdc-typography--body2"><%= message %></span>
      </div>
    </div>
<% end %>


<div class="mdc-card signin-card">
  <div class="mdc-card__primary-action mdc-ripple-upgraded mdc-ripple-upgraded--foreground-activation" tabindex="0">
    <div class="signin-card__primary"><h2 class="signin-card__title mdc-typography--headline6">Register</h2>
      <div class="signin-card__secondary mdc-typography--body2">
        <%= form_with scope: :registration, url: registrations_path, id: "new-session", data: {controller: "new-registration", action: "ajax:success->new-registration#create"} do |form| %>
            <div>
              <div class="mdc-text-field mdc-text-field--fullwidth">
                <%= form.text_field :username, class: "mdc-text-field__input", placeholder: "Username", autofocus: true, autocapitalize: "none" %>
              </div>
            </div>

            <div>
              <div class="mdc-text-field mdc-text-field--fullwidth">
                <%= form.text_field :nickname, class: "mdc-text-field__input", placeholder: "Credential Nickname" %>
              </div>
            </div>

            <div class="center">
              <%= form.submit "Register using WebAuthn", class: "mdc-button mdc-button--raised" %>
            </div>
        <% end %>
      </div>
    </div>
    <div class="mdc-card__actions">
      <div class="mdc-card__action-buttons">
        <div class="mdc-card__actions">
          <%= link_to 'Sign in', new_session_path, class: "mdc-button mdc-card__action mdc-card__action--button mdc-ripple-upgraded" %>
        </div>
      </div>
    </div>
  </div>
</div>
